package com.clearspring.analytics.stream.cardinality;

import com.clearspring.analytics.stream.cardinality.HyperLogLogPlus;
import org.junit.Assert;
import org.junit.Test;
import org.junit.rules.Timeout;
import org.powermock.reflect.Whitebox;

public class HyperLogLogPlus_offerHashedTest_4_Test {
//failed_run   @Test
//  public void offerHashedTest() throws Exception {
//    // Arrange
//    HyperLogLogPlus hyperLogLogPlus = Whitebox.newInstance(HyperLogLogPlus.class);
//    long hashedLong = 1L;
//
//    // Act
//    boolean actual = hyperLogLogPlus.offerHashed(hashedLong);
//
//    // Assert
//    Assert.assertFalse(actual);
//  }
  @org.junit.Rule
  public Timeout timeout = new Timeout(10000);
}
